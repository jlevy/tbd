dist
node_modules
pnpm-lock.yaml
*.min.js
*.min.css
*.md
coverage
.git
.tbd
.tbd-sync
attic
.changeset
# Transient worktree checkouts for background agents. Scanning them makes
# quality checks depend on another agent's in-flight edits, and `pnpm format`
# would rewrite those files underneath it.
.claude/worktrees
