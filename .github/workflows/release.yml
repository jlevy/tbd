name: Release

on:
  push:
    tags:
      - 'v*'

permissions:
  contents: write
  pull-requests: write
  id-token: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Extract version from tag
        id: version
        run: |
          version="${GITHUB_REF#refs/tags/v}"
          echo "version=${version}" >> "$GITHUB_OUTPUT"
          echo "TBD_VERSION_OVERRIDE=${version}" >> "$GITHUB_ENV"

      - uses: pnpm/action-setup@v4

      - uses: actions/setup-node@v6
        with:
          node-version: 24
          cache: pnpm
          registry-url: 'https://registry.npmjs.org'

      - run: pnpm install --frozen-lockfile

      - run: pnpm audit --prod

      - run: pnpm build

      - run: pnpm publint

      - name: Verify packed web command
        run: pnpm --filter get-tbd qa:web-package

      - name: Verify release metadata
        run: pnpm exec tsx packages/tbd/scripts/verify-release-metadata.ts "${GITHUB_REF_NAME}"

      - name: Verify release checkout is clean
        run: |
          status="$(git status --porcelain=v1 --untracked-files=all)"
          if [[ -n "${status}" ]]; then
            printf '%s\n' "${status}"
            exit 1
          fi

      # Resolve the release body BEFORE publishing: if the wrapper or changelog path
      # ever fails, fail here rather than after the package is already on npm.
      - name: Extract changelog for this version
        id: changelog
        run: |
          # Body extraction lives in a unit-tested module (src/utils/changelog.ts,
          # tested in tests/extract-changelog.test.ts) invoked via a thin script, not
          # inline shell, so the logic can be tested and debugged outside of CI. The
          # shell here only wires the script output into the GITHUB_OUTPUT heredoc,
          # using a random delimiter so a changelog line equal to "EOF" can't corrupt it.
          DELIM="body_$(openssl rand -hex 8)"
          {
            echo "body<<${DELIM}"
            pnpm exec tsx packages/tbd/scripts/extract-changelog.ts "${{ steps.version.outputs.version }}"
            echo "${DELIM}"
          } >> "$GITHUB_OUTPUT"

      - name: Publish to npm
        run: pnpm -r publish --access public --no-git-checks
        env:
          NODE_AUTH_TOKEN: ${{ secrets.NPM_TOKEN }}
          NPM_CONFIG_PROVENANCE: true

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          body: ${{ steps.changelog.outputs.body }}
          prerelease: ${{ contains(steps.version.outputs.version, '-') }}
