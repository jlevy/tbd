---
type: is
id: is-01m2pr2c6gds2vz3y9x0nxdwka
title: "P2: Generate tier agent definitions as setup surfaces"
kind: task
status: closed
priority: 1
version: 7
spec_path: docs/project/specs/active/plan-2026-09-16-pr-review-lifecycle-and-agent-delegation.md
delegate: claude-code@spud10.local
labels:
  - exec:judgment
dependencies:
  - type: blocks
    target: is-01m2q1n82ewxqyytff9f2sftj2
parent_id: is-01m2ppwdem47zfrrfhh1rgbvzp
hold: null
hold_until: null
created_at: 2026-09-17T03:55:20.143Z
updated_at: 2026-09-17T11:24:04.587Z
started_at: 2026-09-17T10:59:04.130Z
closed_at: 2026-09-17T11:24:04.586Z
close_reason: "Verified by the coordinator after a rebuild: 131 tests in 9 files (setup-tier-agents, setup-flows, doctor, setup-policy-grants, golden-output, integration-files, doc-categories, dry-run) and 62 tryscript cases (setup, setup-commands, uninstall, orientation golden) passed; typecheck, eslint, prettier, flowmark clean; committed"
resolution: null
duplicate_of: null
---
Plan: Design > Tier Agent Definitions; Implementation Plan Phase 2 item "Add tier agent definitions to setup as independent surfaces, with setup and golden tests"; Testing Strategy > Setup surfaces. Decision made: generated by default.

What: `tbd setup` generates four definitions: tbd-strong-max (`max`), tbd-strong (`xhigh`), tbd-moderate (`xhigh`), and tbd-fast (`medium`).
- .claude/agents/tbd-*.md: `model` and `effort` frontmatter, and a short body (follow the brief, run the named tbd shortcut, report in the requested format).
- .codex/agents/tbd-*.toml: `model`, `model_reasoning_effort`, and `developer_instructions`.
Models and levels come from the dated suggestions in agent-model-tiers, using the identifiers and TOML fields verified in tbd-6e2u. Stamp them as managed, so setup refreshes them on upgrade and never overwrites a user-owned file. Make them setup surfaces that can be selected or excluded with `--surfaces` independently of the skill surfaces (new surface IDs, for example claude-agents and codex-agents). `tbd uninstall` removes them. Document the surfaces in the CLI manual.
Decision to make and report (judgment): "turned off" can only mean excluding the surfaces with `--surfaces` on each run, since there is no persisted surface selection today, unless you add a persisted opt-out. Keep the change small.

Write set: packages/tbd/src/cli/commands/setup.ts; packages/tbd/src/lib/integration-paths.ts; packages/tbd/src/cli/commands/uninstall.ts; packages/tbd/src/cli/commands/doctor.ts (only if managed-surface checks must cover the new files); packages/tbd/docs/tbd-docs.md; packages/tbd/tests/setup-flows.test.ts (--surfaces selector); packages/tbd/tests/setup-tier-agents.test.ts (new, golden content); packages/tbd/tests/cli-setup-commands.tryscript.md (setup help); packages/tbd/tests/cli-uninstall.tryscript.md if affected. Do not add generated files to this repository (packaging bead tbd-sqat).

Acceptance (Testing Strategy > Setup surfaces): golden content of all eight files; refresh on upgrade; turning the surfaces off; a user-owned file is not overwritten; uninstall removes them. Run `pnpm --filter get-tbd exec vitest run tests/setup-tier-agents.test.ts tests/setup-flows.test.ts` and `pnpm --filter get-tbd exec tryscript run tests/cli-setup-commands.tryscript.md tests/cli-uninstall.tryscript.md`; typecheck, prettier, eslint; Markdown check on tbd-docs.md.

Rules: the coordinator commits and syncs. Do not commit, push, run `tbd sync`, or edit the plan spec unless the write set lists it. Keep scratch files in the session scratch directory. Run only the targeted checks listed, not the full suite.
Markdown check: `uvx --exclude-newer-package flowmark-rs=2026-05-31 flowmark-rs@0.3.1 --auto --check <files>` (per-file form of `pnpm format:md:check`). TypeScript checks: `pnpm exec prettier --check <files>`, `pnpm exec eslint <files>`, `pnpm --filter get-tbd typecheck`. Tests that read `packages/tbd/dist/` (integration-files, doc-references, setup-flows, golden-output, tryscripts) need a current build: run `pnpm --filter get-tbd build` once if dist is stale.

## Notes

From tbd-ycxf deviation row 8: vendor guidance suggests agent definitions preload skills, memory, and a detailed description; tbd-* definitions currently set only model and level. Decide whether the generated tier definitions should include a description that routes work to them and point to delegate-to-subagents in their body.
